/*
*	Author:			Michael Terry
*	Date:			2/15/2023
*	Description:	This program will begin by reading a file that conainins a list of all grocery items that have been
*					purchased. The items are then counted and a new file is created containing each item along with its
					quantity. The user is then shown an options menu that allows them to print the quantity for one item,
					print the quantity for all items, print a histogram of all items, or quit the program.
*/

#include "DisplayMenuFunction.h"

int main() {
	// calls the displayMenu function which starts the program
	displayMenu();

	return 0;
}
